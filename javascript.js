// operators: + - * /

switch(operator){
    case '+':
        return;
        break;
    case '-':
        return;
    case '*':
        return;
    case '/':
        return;
}

// can manipulate total until clear is hit
let total = 0;

// clear function: reset total back to 0
function clear(){
    return total = 0;
}